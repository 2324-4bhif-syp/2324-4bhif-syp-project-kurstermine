quarkus.package.type=uber-jar
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:4200
# quarkus.http.root-path=/api

# datasource configuration
quarkus.datasource.db-kind = postgresql
quarkus.datasource.username = app
quarkus.datasource.password = app
quarkus.datasource.jdbc.url = jdbc:postgresql://localhost:5432/db

# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.database.generation=drop-and-create

quarkus.keycloak.admin-client.server-url=https://babyapp.ddns.net/keycloak
quarkus.oidc.auth-server-url=https://babyapp.ddns.net/keycloak/realms/htl
quarkus.oidc.client-id=quarkus-service
quarkus.oidc.credentials.secret=1f9M7z1QjSfkC5fIjfYNCikO4RU2MCtk
quarkus.oidc.tls.verification=none

# Realm name for Keycloak
realm.name=htl